# Frontend: Deployment + Service (Nginx serving Vite-built SPA)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: smart-city-frontend
  namespace: smart-city
  labels:
    app: smart-city-frontend
    tier: frontend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: smart-city-frontend
  template:
    metadata:
      labels:
        app: smart-city-frontend
        tier: frontend
    spec:
      containers:
        - name: frontend
          image: smart-city/frontend:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          resources:
            requests:
              cpu: 20m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 2
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: smart-city-frontend
  namespace: smart-city
  labels:
    app: smart-city-frontend
spec:
  type: ClusterIP
  selector:
    app: smart-city-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
