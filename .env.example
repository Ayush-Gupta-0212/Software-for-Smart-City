# Copy to `.env` (gitignored) before running `docker compose build`.
# These are passed as Vite build-args to the frontend image.
# Backend env vars live in ./backend/.env (separately).

VITE_RAZORPAY_KEY_ID=
VITE_MAPBOX_ACCESS_TOKEN=
VITE_GOOGLE_MAPS_API_KEY=
